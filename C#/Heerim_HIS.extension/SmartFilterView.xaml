<Window x:Class="Heerim_HIS.SmartFilterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Heerim HIS Smart Filter" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Border Background="White" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#1A73E8" CornerRadius="10,10,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="15,10">
                    <TextBlock Text="Smart Filter" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                    <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" Foreground="White" BorderThickness="0" Click="Close_Click"/>
                </Grid>
            </Border>

            <!-- Search Area -->
            <StackPanel Grid.Row="1" Margin="15,15,15,0">
                <TextBlock Text="Search Elements" FontSize="12" Foreground="#5F6368" Margin="0,0,0,5"/>
                <Grid>
                    <TextBox x:Name="SearchBox" Height="32" Padding="5,0" VerticalContentAlignment="Center" BorderBrush="#DADCE0" KeyUp="SearchBox_KeyUp"/>
                    <TextBlock IsHitTestVisible="False" Text="Category or Family Name..." VerticalAlignment="Center" Margin="10,0" Foreground="#BDC1C6">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Button Content="Search" Margin="0,10,0,0" Height="30" Background="#1A73E8" Foreground="White" BorderThickness="0" Click="Search_Click"/>
            </StackPanel>

            <!-- Results -->
            <ListBox Grid.Row="2" x:Name="ResultListBox" Margin="15,15,15,0" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{Binding Name}" FontWeight="Medium" FontSize="13"/>
                            <TextBlock Text="{Binding Category.Name}" FontSize="11" Foreground="#5F6368"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Footer -->
            <Border Grid.Row="3" Margin="15" BorderBrush="#F1F3F4" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Select All" Width="100" Height="30" Background="#E8F0FE" Foreground="#1A73E8" BorderThickness="0" Click="SelectAll_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
